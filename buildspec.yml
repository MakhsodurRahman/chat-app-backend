version: 0.2

phases:
  build:
    commands:
      - echo Syncing source code to S3 for local build...
      - aws s3 sync . s3://$ARTIFACT_BUCKET/latest-source/ --delete

artifacts:
  files:
    - deploy-buildspec.yml
